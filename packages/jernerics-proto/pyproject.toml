[project]
name = "jernerics-proto"
version = "0.1.0"
description = "Shared protobuf definitions for jernerics"
requires-python = ">=3.12"
dependencies = [
    "protobuf>=7.34.1",
]

[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"
