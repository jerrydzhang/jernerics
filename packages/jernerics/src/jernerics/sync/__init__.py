"""Continuous project-source synchronization.

This is filesystem sync of the *project source* (the code you edit) between the
local machine and a remote host, for interactive HPC sessions. It is distinct
from the tracking pipeline (``jernerics/tracking``) and from the ``jernerics
sync`` CLI command, which replays tracked events to the server — see
``CONTEXT.md`` for the vocabulary split.
"""
